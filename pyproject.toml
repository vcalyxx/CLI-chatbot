[project]
name = "cli-chatbot"
version = "0.1.0"
description = "A simple terminal chatbot built with Claude that supports streaming and tool calling"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic[bedrock]>=0.96.0",
    "python-dotenv>=1.2.2",
]
